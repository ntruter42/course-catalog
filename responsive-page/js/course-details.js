// Create variable for all course elements
// Get each element using a for loop
// Add an event listener for each course item button
// 


const courseElements = document.querySelectorAll(".course-item");

for (let courseItem of courseElements) {
	const detailsButton = courseItem.querySelector(":scope > button");
	// Work in progress
}
